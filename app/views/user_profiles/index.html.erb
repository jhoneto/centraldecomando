<div class="block">
  <div class="secondary-navigation">
    <ul class="wat-cf">
      <li class="first active"><%= link_to "#{t("web-app-theme.list", :default => "List")}", user_profiles_path  %></li>
      <li><%= link_to "#{t("web-app-theme.new", :default => "New")}", new_user_profile_path %></li>
    </ul>
  </div>          
  <div class="content">          
    <h2 class="title"><%= t("web-app-theme.all", :default => "All")  %> UserProfiles</h2>
    <div class="inner">
      <table class="table">
        <tr>             
          <th class="first">ID</th>
                    <th>
            <%= t("activerecord.attributes.user_profile.user_id", :default => t("activerecord.labels.user_id", :default => "User_id")) %>
          </th>
                    <th><%= t("web-app-theme.created_at", :default => "Created at")  %></th>
          <th class="last">&nbsp;</th>
        </tr>
        <% @user_profiles.each do |user_profile| -%>
        <tr class="<%= cycle("odd", "even") %>">
          <td>
            <%= user_profile.id %>
          </td>
                    <td>
            <%= link_to user_profile.user_id, user_profile_path(user_profile) %>
          </td>
                    <td>
            <%= user_profile.created_at %>
          </td>
          <td class="last">
            <%= link_to "#{t("web-app-theme.show", :default => "Show")}", user_profile_path(user_profile) %> |
            <%= link_to "#{t("web-app-theme.edit", :default => "Edit")}", edit_user_profile_path(user_profile) %> |
            <%= link_to "#{t("web-app-theme.delete", :default => "Delete")}", user_profile_path(user_profile), :method => :delete, :confirm => "#{t("web-app-theme.confirm", :default => "Are you sure?")}" %>
          </td>
        </tr>          
        <% end -%>
      </table>
      <div class="actions-bar wat-cf">
        <div class="actions">
        </div>
        
      </div>
    </div>
  </div>
</div>

<% content_for :sidebar, render(:partial => 'sidebar') -%>
