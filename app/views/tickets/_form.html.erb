<%= semantic_form_for @ticket do |form| %>
	<%= form.inputs :ticket_type, :project, :client, :solicitant, :title, :description %>
	<%= form.buttons do %>
		<%= form.commit_button "Cadastrar"%>
	<% end %>
<% end %>
